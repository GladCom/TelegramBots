name: Pull request build

on:
  pull_request:
    branches:
      - dev
      - master

jobs:
  pull_request_build:
    uses: GladCom/TelegramBots/.github/workflows/build.yml@master
    with:
      solution-path: ./TelegramBots/TelegramBots.sln